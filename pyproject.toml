[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pygaminal"
version = "0.1.3"
description = "Pygame framework to manage entity component system (ECS) easily."
dependencies = [
  "pygame"
]